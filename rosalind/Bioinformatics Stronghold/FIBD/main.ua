~ "../utils.ua" ~ BigAdd BigToString
# Separate numbers into two parts to bypass float limits
&fras "input.txt"
°⊟ ⊜⋕ ¬ ∊:"\s\n".

⊙(≡(□¤)⊂1⊚-1)
-1

□/◇BigAdd⍥(⍜⊢⋅∘⊃(↻¯1|□/◇BigAdd↘1))

&pBigToString °□
